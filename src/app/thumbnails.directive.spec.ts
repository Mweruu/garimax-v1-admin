import { ThumbnailsDirective } from './thumbnails.directive';

describe('ThumbnailsDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumbnailsDirective();
    expect(directive).toBeTruthy();
  });
});
